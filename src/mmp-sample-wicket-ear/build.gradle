apply plugin: 'ear'

dependencies {
  deploy project(path:':mmp-sample-wicket-war', configuration:'archives')
}

ear {
  deploymentDescriptor { 
    webModule(":mmp-sample-wicket-war", "/sample") 
  }
}
